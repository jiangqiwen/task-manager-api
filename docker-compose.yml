version: "3.9"

services:
  api:
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    # image: task-manager-api:local
    image: task-manager-api-task3
    container_name: task-manager-api-task3
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    restart: unless-stopped